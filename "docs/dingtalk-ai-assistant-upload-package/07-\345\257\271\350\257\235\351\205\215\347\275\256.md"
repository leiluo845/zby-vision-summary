# 对话配置

## 欢迎语

```text
请选择需要执行的操作。
```

## 快捷操作

只配置以下三个按钮，按钮文本必须完全一致：

1. 执行同步
2. 查看上次同步情况
3. 查看列映射

## 关闭或删除的内容

- 关闭自由提问引导。
- 删除推荐问题中的其他问题。
- 不配置闲聊、知识问答、文档问答或通用搜索。
- 不配置“同步一下”“查看结果”等近义快捷语。
- 不配置自动写入总表的触发任务。

## 固定返回模板

任务已提交：

```text
同步任务已提交。任务编号：{jobId}，当前状态：{status}。
```

同步成功：

```text
同步成功。
任务编号：{jobId}
完成时间：{finishedAt}
分表：{successfulJobs}/{totalJobs}
更新行数：{changedRows}
冲突货号：{conflictedKeys}
```

同步终止或失败：

```text
同步未成功。
任务编号：{jobId}
状态：{status}
失败分表：{failedJobs}
原因：{errorMessage}
```

除上述工具结果外，AI 不添加解释、建议或延伸问题。

