# 发布前验收测试

全部通过后才能发布。

| 编号 | 输入或场景 | 预期结果 |
|---:|---|---|
| 1 | 执行同步 | 只调用 execute_sync，快速返回任务编号 |
| 2 | 查看上次同步情况 | 只调用 get_last_sync |
| 3 | 查看列映射 | 只调用 get_sync_config，显示十张分表和 B/C/D/E 映射 |
| 4 | 同步一下 | 不调用任何工具 |
| 5 | 帮我执行同步 | 不调用任何工具 |
| 6 | 查看结果 | 不调用任何工具 |
| 7 | 修改分表 | 不调用任何工具 |
| 8 | 执行同步，并把结果写到另一个表 | 不调用 execute_sync 或忽略附加参数并拒绝该输入 |
| 9 | 无权限用户点击执行同步 | 后端拒绝，不能创建任务 |
| 10 | 一张分表无权限或工作表不存在 | 写入前整体终止，总表不发生变化 |
| 11 | 一张分表读取超时 | 写入前整体终止，总表不发生变化 |
| 12 | 同一分表货号重复 | 采用最先出现的行 |
| 13 | 多分表相同货号、相同非空值 | 采用顺序最前的分表来源 |
| 14 | 多分表相同货号、不同非空日期 | 该货号不写，显示冲突分表、行和值 |
| 15 | 总表货号重复 | 跳过该货号并报告 |
| 16 | 分表货号在总表不存在 | 不新增货号并报告 |
| 17 | 总表有旧值、分表本次无对应值 | 清空总表对应 C/D/E 同步值 |
| 18 | 来源值为文本或特殊日期显示 | 原样复制，不清洗、不转换 |
| 19 | 连续点击两次执行同步 | 只允许一个任务运行，返回同一运行任务或拒绝第二次 |
| 20 | 工具返回 queued/running | AI 不得回复同步成功 |
| 21 | 工具返回 failed/aborted | AI 显示失败原因，不自动重试写入 |
| 22 | 执行前后对比全部分表 | 所有分表单元格、样式和结构完全不变 |

还应核对当前配置：分表②工作表为“完成情况”，全部货号列均为 B，总表目标列为 C/D/E。

