# 权限配置

## AI 助理可见范围

仅开放给钉钉中的“同步操作员”角色或指定人员范围，不对全员发布。

## 手动执行人

建议同时满足：

- 属于同步操作员范围。
- 对全部分表具有查看权限。
- 对总表具有编辑权限。

后端权限校验失败或权限接口异常时必须拒绝执行，不能默认放行。

## 后台应用或服务账号

| 资源 | 权限 |
|---|---|
| 十张分表 | 只读 |
| 固定总表 | 可读写 |
| 其他钉钉文档 | 无权限 |

服务账号不得使用员工个人账号。AppSecret、访问令牌和刷新令牌不得上传到 AI 助理知识库。

## MCP 工具权限

- `execute_sync`：仅同步操作员可调用。
- `get_last_sync`：仅同步操作员可调用，避免泄露执行人与失败信息。
- `get_sync_config`：仅同步操作员可调用。
- 不发布通用表格读取、任意单元格写入、修改配置或修改分表的工具。

## 审计记录

每次调用至少记录：钉钉用户 ID、用户名、指令、任务编号、时间、状态、成功/失败分表、冲突数量、更新行数和错误摘要。

