# 钉钉多表同步 AI 助理配置包

## 助理名称

多表同步助手

## 配置顺序

1. 在“人设 > 角色与设定”中粘贴 `01-粘贴到人设-角色与设定.txt` 的全部内容。
2. “人设 > 兜底回复”优先留空。如果平台要求必填，粘贴 `02-仅在必填时粘贴到兜底回复.txt`。
3. 在“知识”中只上传 `04-直接上传到知识库-多表同步助手知识.md`。
4. 在“Skill”中新建一个名为“多表同步控制”的 Skill，粘贴 `03-粘贴到Skill-多表同步控制.txt`。
5. 在“MCP”中接入后端提供的三个工具。工具契约见 `05-MCP工具契约与后端要求.md`，机读定义见 `09-MCP工具定义.json`。
6. “工作流”不需要配置。
7. “触发任务”不需要配置，禁止创建自动写入总表的触发任务。
8. 按 `07-对话配置.md` 设置欢迎语和三个快捷操作。
9. 按 `06-权限配置.md` 限制使用人和服务账号权限。
10. 发布前执行 `08-发布前验收测试.md` 中的全部测试。

## 只允许的三个操作

- 执行同步
- 查看上次同步情况
- 查看列映射

不使用模糊表达，不回答其他问题，不接受表格链接、工作表名、列号或同步参数。

## 上线前必须完成的后台改造

当前本地网页服务不能直接作为钉钉 AI 助理 MCP 使用。上线前必须完成以下事项：

- 部署到钉钉可访问的公司 HTTPS 域名，不能使用 `127.0.0.1`。
- 实现并暴露 `execute_sync`、`get_last_sync`、`get_sync_config` 三个 MCP 工具。
- 验证钉钉用户身份和同步操作权限。
- `execute_sync` 改为快速返回任务编号，后台异步执行。
- 点击“执行同步”本身即代表明确执行，不再进入额外的聊天确认流程。
- 任意一张分表读取失败时，在清空或写入总表前整体终止。
- 保留固定总表写入白名单，所有分表只读。

## 当前配置事实源

本配置包依据 `src/config/sync-plans.json` 生成。旧需求文档中的以下内容已经废弃，不得再使用：

- 分表②旧工作表名“完成情况0716”。当前为“完成情况”。
- 部分分表货号列为 A、P 或 C 的旧配置。当前所有总表和分表货号列均为 B。

