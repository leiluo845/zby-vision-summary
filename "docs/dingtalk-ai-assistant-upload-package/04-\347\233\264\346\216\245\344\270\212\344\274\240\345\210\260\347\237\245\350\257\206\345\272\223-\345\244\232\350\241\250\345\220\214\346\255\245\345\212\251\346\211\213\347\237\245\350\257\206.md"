# 多表同步助手知识

## 支持的操作

本助理只支持三个固定操作：

1. 执行同步
2. 查看上次同步情况
3. 查看列映射

其他问题不属于本助理的处理范围。

## 总表配置

- 同步计划：总表同步
- 总表工作表：Sheet1
- 货号列：B
- 齐色主附图完成时间：C
- A+完成时间：D
- 视频完成时间：E

只允许写入总表配置指定的 C、D、E 同步列。不得修改总表货号列及其他列。

## 当前分表顺序和列映射

当前共有十张分表。所有分表的工作表名称均为“完成情况”，货号列均为 B。

| 顺序 | 分表 | 工作表 | 货号 | 齐色主附图完成时间 | A+完成时间 | 视频完成时间 | 总表目标 |
|---:|---|---|---|---|---|---|---|
| 1 | 分表① 欧洲站上新加色优化表 | 完成情况 | B | C | D | E | C / D / E |
| 2 | 分表② 新流程时间轴 SDM-YSF | 完成情况 | B | C | D | E | C / D / E |
| 3 | 分表③ Tankaneo 上新表 | 完成情况 | B | C | D | E | C / D / E |
| 4 | 分表④ RF新流程流转表2026 | 完成情况 | B | C | D | E | C / D / E |
| 5 | 分表⑤ 鞋子秋冬新款页面进度 | 完成情况 | B | C | D | E | C / D / E |
| 6 | 分表⑥ 吴旻轩组新款流转 | 完成情况 | B | C | D | E | C / D / E |
| 7 | 分表⑦ TK上架对接表（新）6月至今 | 完成情况 | B | C | D | E | C / D / E |
| 8 | 分表⑧ 日常任务对接表-（拓展+TK） | 完成情况 | B | C | D | E | C / D / E |
| 9 | 分表⑨ Haloumoning-内部时效对接表格 | 完成情况 | B | C | D | E | C / D / E |
| 10 | 分表11 AOYSKY 新品流程表 | 完成情况 | B | C | D | E | C / D / E |

字段对应关系固定为：

- 分表 C 列“齐色主附图完成时间”对应总表 C 列。
- 分表 D 列“A+完成时间”对应总表 D 列。
- 分表 E 列“视频完成时间”对应总表 E 列。

## 执行同步规则

1. 每次执行都重新读取总表和全部十张分表，不使用上一次读取的数据。
2. 分表只能读取，任何情况下都不得修改分表。
3. 任意一张分表读取失败时，在清空或写入总表之前整体终止本次同步。
4. 以货号进行精确匹配，不在总表中自动新增货号。
5. 同一分表中货号重复时，取最先出现的行。
6. 同一货号在多张分表出现且同一字段值相同时，按分表顺序采用最先出现的来源。
7. 同一货号在多张分表中为同一字段提供不同的非空日期时，认定为冲突。该货号三个同步字段均不写入，并记录冲突字段、分表、行号和值。
8. 分表单元格显示什么就复制什么，不进行日期转换、格式清洗、有效性校验或自动纠正。
9. 每次执行都以本次读取结果重建总表 C、D、E 列的同步值。分表中没有对应值的总表同步单元格应清空。
10. 总表中货号重复时，跳过该货号并记录失败原因。
11. 分表有货号但总表没有时，不新增货号，记录为总表缺少该货号。
12. 只有后台工具明确返回 succeeded，才能认定同步成功。

## 状态含义

| 状态 | 含义 |
|---|---|
| queued | 任务已进入队列，尚未完成 |
| running | 正在重新读取分表或写入总表 |
| succeeded | 全部要求已完成且没有失败 |
| completed_with_errors | 任务完成，但存在冲突货号或其他可报告失败 |
| aborted | 在写入前因权限、分表读取失败或安全检查未通过而终止 |
| failed | 执行过程中失败，不能认定同步成功 |

## 安全边界

- 不允许用户更换总表、分表、工作表或列映射。
- 不允许用户传入任意表格链接或单元格范围。
- 不允许 AI 助理直接操作单元格。
- 所有写入必须经过固定总表和固定工作表白名单校验。
- 分表读取权限和总表编辑权限必须由后台实时校验。
- 列映射的实时事实来源是 get_sync_config 工具，不是本知识文档。

