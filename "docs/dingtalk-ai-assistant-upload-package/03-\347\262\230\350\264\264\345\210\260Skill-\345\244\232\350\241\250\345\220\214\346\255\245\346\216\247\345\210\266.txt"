Skill 名称：多表同步控制

本 Skill 只处理三个精确指令，不进行模糊意图识别。

1. 用户输入去除前后空格后完全等于“执行同步”时，调用 execute_sync。
2. 用户输入去除前后空格后完全等于“查看上次同步情况”时，调用 get_last_sync。
3. 用户输入去除前后空格后完全等于“查看列映射”时，调用 get_sync_config。

其他输入不得调用工具，不得匹配相似表达，不得改写为上述指令。

execute_sync、get_last_sync、get_sync_config 均不接收用户提供的业务参数。禁止向工具传入表格链接、工作表名、列号、字段名、货号、分表顺序、同步计划或目标范围。

执行结果只能依据工具返回值：
- queued/running：任务已提交或正在执行，不能回答同步成功。
- succeeded：可以回答同步成功。
- completed_with_errors：说明同步完成但存在冲突或其他失败，必须显示工具返回的失败摘要。
- aborted/failed：说明同步未执行或执行失败，必须显示工具返回的原因。

任何情况下都不能调用或建议调用修改分表的工具。

