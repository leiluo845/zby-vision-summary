# MCP 工具契约与后端要求

## 总体原则

AI 助理只获得三个固定工具。所有业务参数均由服务端配置决定，模型不能选择文档、工作表、列、货号或同步计划。

钉钉身份必须由受信任的请求上下文或服务端会话取得，不能接受模型传入的 `userId` 作为授权依据。

## execute_sync

用途：执行固定的多表同步计划。

输入：无业务参数。

服务端处理顺序：

1. 校验调用来源和钉钉用户身份。
2. 校验用户属于同步操作员，并满足业务授权要求。
3. 获取全局执行锁；已有任务运行时返回 `running` 和现有任务编号。
4. 创建任务并在数秒内返回 `jobId` 和 `queued`，不得等待整次同步完成。
5. Worker 重新读取总表和全部十张分表。
6. 任意分表读取失败时，将任务标记为 `aborted`，不得清空或写入总表。
7. 全部分表读取成功后计算重复、冲突和目标值。
8. 仅向固定总表、固定工作表和固定 C/D/E 列写入。
9. 保存完整运行记录。

“执行同步”按钮本身即为明确执行操作，不再要求用户发送第四个确认指令。空白值按当前业务规则清空总表对应同步值，不进入聊天确认流程。

最小返回字段：

```json
{
  "jobId": "server-generated-id",
  "status": "queued",
  "submittedAt": "2026-07-23T10:00:00+08:00"
}
```

## get_last_sync

用途：返回当前正在执行的任务；没有当前任务时返回最近一次已结束任务。

输入：无业务参数。

最小返回字段：

```json
{
  "jobId": "server-generated-id",
  "status": "succeeded",
  "startedAt": "2026-07-23T10:00:00+08:00",
  "finishedAt": "2026-07-23T10:00:30+08:00",
  "triggerUserName": "操作人",
  "totalJobs": 10,
  "successfulJobs": 10,
  "failedJobs": [],
  "conflictedKeys": 0,
  "changedRows": 120,
  "errorMessage": null
}
```

读取失败的分表必须返回分表标签和原始错误摘要。

## get_sync_config

用途：返回服务端当前生效的总表和分表列映射。

输入：无业务参数。

返回内容至少包含：配置版本、总表工作表、总表 B/C/D/E 列、十张分表的固定顺序、分表名称、工作表名称、货号列和三个数据列。

返回内容不得包含访问令牌、AppSecret、Cookie、服务账号凭证或内部密钥。

## 后台强制保护

- 分表客户端不得包含任何写方法，或必须通过资源级写入白名单确保所有分表节点永远不可写。
- 总表写入前再次校验 nodeId 和 sheetId 白名单。
- 任意分表读取失败必须 fail closed，在写入前整体终止。
- 写操作使用幂等任务编号，防止重复回调造成重复执行。
- 同一同步计划只允许一个任务运行。
- MCP 接口必须鉴权、限流并记录调用人。
- 所有凭证只保存在服务端密钥管理中。

## 当前代码的接入差异

现有本地服务可以提供配置、执行和上次结果数据，但尚不满足正式 MCP 接入要求：网页执行接口没有钉钉免登鉴权，回调尚未验签，正式客户端权限检查未真正实现，执行是同步等待模式，且个别分表读取失败时可能继续处理其他分表。

这些问题修复前，不得把 MCP 的 `execute_sync` 开放给业务人员。

